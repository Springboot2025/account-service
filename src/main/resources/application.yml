spring:
  datasource:
    url: jdbc:postgresql:///legalpro?cloudSqlInstance=halogen-episode-470910-h9:us-central1:legalpro-postgres&socketFactory=com.google.cloud.sql.postgres.SocketFactory
    username: legalpro
    password: legalPro42#
    driver-class-name: org.postgresql.Driver
    hikari:
      initialization-fail-timeout: 0  # Start immediately even if DB not ready

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

jwt:
  secret: legalPro42
  expirationMs: 86400000

logging:
  level:
    org.springframework.web.multipart: DEBUG
    org.apache.catalina.core: DEBUG

server:
  port: ${PORT:8080}  # Use Cloud Run injected port
  address: 0.0.0.0
