spring:
  datasource:
    url: jdbc:postgresql:///legalpro?cloudSqlInstance=ecstatic-seeker-474703-d4:us-central1:legalpro-postgres&socketFactory=com.google.cloud.sql.postgres.SocketFactory
    username: legalpro
    password: legalPro42#
    driver-class-name: org.postgresql.Driver
    hikari:
      initialization-fail-timeout: 0

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

jwt:
  secret: legalPro42
  expirationMs: 86400000

logging:
  level:
    org.springframework.web.multipart: DEBUG
    org.apache.catalina.core: DEBUG

server:
  port: ${PORT:8080}
  address: 0.0.0.0

google:
  api:
    key: AIzaSyCwhQXBItzkEzJWEQtCs8RCz5ezAZuYq7c
